[pytest]
testpaths = tests
markers =
    integration: exercises the full CV pipeline end to end; slower than a unit test.
# Any single test that runs longer than this is hung, not slow.
timeout = 600
